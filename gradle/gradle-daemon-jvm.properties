# Pins the JVM that *runs* Gradle, not just the toolchain that compiles sources.
# build-logic depends on Loom 1.17, whose module metadata requires a Java 21
# launcher; without this pin, an older JDK fails with a cryptic resolution error.
toolchainVersion=21
